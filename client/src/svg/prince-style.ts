/** Prince artwork units (800 × 1000), separate from the chart's stroke scale. */
export const ACHIEVEMENT_STYLE = {
  columns: 12,
  top: 926,
  gap: 644 / 11,
  scale: 0.65,
  radius: 21,
  stroke: 1.6,
  outlineOpacity: 0.8,
  markOpacity: 0.9,
} as const;
